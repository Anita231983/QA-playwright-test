# Copy this file to .env and fill in real values.
# In CI, these are provided via GitHub Actions secrets (see .github/workflows...)

# Credentials used for both UI login and API login (/api/accounts/login/)
TEST_EMAIL=admin@chairlyo.com
TEST_PASSWORD=adminpassword

# UI application host
UI_BASE_URL=https://stage.chairlyo.com

# API host
API_BASE_URL=https://base.chairlyo.com